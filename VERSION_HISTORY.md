# 旅遊手冊專案 - 版本演進與實體操作指南

---

## 🎯 專案目的與實體操作指南 (隨功能同步增減)

### 1. 專案目的
本專案旨在為喜愛旅行的您，打造一個**美觀、高自主性、且便於管理的多國旅行手冊系統**。
您可以在出發前與 Gemini 討論出行程，將其導入系統進行微調、確認行李打包進度、並試算信用卡刷卡回饋；在旅行時，可直接將手冊以 **9 頁 A4 規格**列印成紙本，隨身攜帶，作為在各大景點/車站**親手蓋上紀念戳章**與**手寫旅行日記**的實體回憶載體。

---

### 2. 網頁操作指南 (數位端)
*   **切換行程手冊**：在網頁最上方點擊 **「📁 選擇手冊」** 下拉選單，即可一鍵切換不同國家的旅行手冊。
*   **新增行程手冊**：點擊 **「➕ 新增」**，輸入新標題，系統將自動以範本為您建立一本新行程。
*   **自訂編輯內容**：點擊 **「✏️ 編輯內容」** 會開啟編輯器：
    *   *基本設定*：可快速修改主要標題、副標題、日期、航班及出發倒數。
    *   *進階 JSON 編輯*：可在此處微調細節，或將與 Gemini 討論好的行程 JSON 代碼直接複製貼上在此處，點擊 **「儲存修改」** 立即套用。
*   **切換手冊主角 (Mascot)**：在手冊封面頁，使用 **「🐾 選擇手冊主角」** 下拉選單，可動態將整本手冊的主題與封面插圖更換為 🐶 馬爾濟斯、🐱 溫馨貓貓、🐍 萌萌綠蛇 或 🐼 圓滾熊貓（設定會自動記錄，重整網頁不消失）。
*   **行李清單與自由編輯**：在 **「行李清單」** 頁面勾選已打包項目，網頁會自動計算進度條。您可以使用下方的輸入框新增物品，或者**雙擊物品名稱**直接線上修改文字，點擊右側的紅色 **`×`** 按鈕即可刪除該項目（刪除按鈕在列印時會自動隱藏，不影響美觀）。
*   **消費推薦試算**：在 **「刷卡攻略」** 頁面輸入日圓或台幣消費金額與類別，決策器會自動幫您判斷目前該刷「中信 Uniopen」還是「玉山熊本熊卡」最算。

---

### 3. 實體手冊印製指南 (紙本端 - 關鍵操作)
本專案已針對列印進行嚴格排版，確保以下步驟能印出完美的實體手冊：
1.  **開啟手冊預覽**：在網頁左下角點擊 **「手冊預覽模式」**，此時網頁會展開所有頁面，以 A4 紙張比例排版呈現。
2.  **啟動列印對話框**：點擊左下角 **「列印成實體手冊」** 按鈕（或按鍵盤 `Cmd + P` / `Ctrl + P`）。
3.  **瀏覽器列印核心設定 (必做！)**：
    *   **目的地 (Destination)**：選擇 **「另存為 PDF」** (Save as PDF)。
    *   **版面配置 (Layout)**：一般直式版型（單頁經典、純文字）請選擇 **「直向」** (Portrait)；當使用 **「雙頁詳細版 (detailed)」** 或 **「四折頁口袋版 (fourfold)」** 時，系統已動態強制設定為 **「橫向」** (Landscape)！
    *   **邊界 (Margins)**：一般直式版型請選擇 **「預設」**；當使用 **「雙頁詳細版 (detailed)」** 或 **「四折頁口袋版 (fourfold)」** 時，系統已動態將邊界設為 **0 (無邊界)**，以實現最完美的滿版拼合列印！
    *   **背景圖形 (Background graphics)**：**⚠️ 必須勾選！** 否則精美的水彩插圖與卡片背景色將無法印出。
4.  **超商列印操作 (ibon / FamiPort)**：
    *   將儲存好的 PDF 檔案上傳至超商的雲端列印系統。
    *   在超商機台選擇：**「PDF 列印」** -> **「A4 規格」**。
    *   *建議選單*：
        *   **一般直式版型**：可選擇 **雙面列印**（單頁經典/純文字共 9 頁 = 5張紙）。
        *   **雙頁詳細版 (detailed)**：選擇 **雙面列印 (橫向 A4)**（共 9 張 A4，每張包含左右 2 個 A5 頁面，雙面列印僅需 **5張 A4 紙**，印出後對折可裝訂成精緻的雙頁手冊）。
        *   **四折頁口袋版 (fourfold)**：選擇 **雙面列印 (橫向 A4)**（共 9 個面板，雙面列印僅需 **2張 A4 紙**，印出後沿著灰色虛線摺疊成風琴式口袋長條書）。
    *   在行程中，翻到最後一頁 **「旅行紀念戳章收集頁」**。此頁面採用全空白設計，您可以自由發揮，蓋上任何大小或形狀 of 景點與車站實體戳章！

---

## 🚀 版本變更日誌 (Version History)

### 4.9 - 自訂加頁版面大器化與緊湊切齊優化 (Custom Pages Enlarge & Top-Alignment V4.9)
*發布日期：2026-06-14*
- **自訂蓋章頁與雜記頁向上切齊**：移除了原本自訂加頁上方過大的空白邊距，將其 top padding 縮減至 `15px`，使加頁標頭緊湊貼齊 A5 紙面頂端，視覺感更為大器與精緻。
- **雜記頁相片框大幅放大 (260px)**：將旅遊雜記頁上半部的「自訂照片黏貼框」高度由 `200px` 大幅放大至 `260px`（下半部橫線打字區微調至 `200px` 高度），提供更具視覺衝擊力的高清晰照片貼處，並完美通過防跨頁高度上限安全檢測。
- **自訂蓋章頁收集區高度擴增 (420px)**：將動態附加蓋章頁的「純白蓋章收集區」高度由 `350px` 擴增至 `420px`，充分填滿 A5 版面，給予使用者最充裕的戳章與票根黏貼收集空間。

### 4.8 - 自由新增蓋章頁與日記帳雜記頁 (Dynamic Custom Pages & Ruled Travel Notes V4.8)
*發布日期：2026-06-14*
- **自由新增自訂加頁功能**：在側邊欄新增了「自訂加頁」控制區，用戶點擊按鈕即可在手冊最尾端動態附加「自訂蓋章頁」或「旅遊雜記頁」，滿足多個印章收集景點與額外日程手記的需求。
- **自訂蓋章頁 (Physical Stamps Page)**：點擊新增後可無限增加純白蓋章收集頁面，方便在旅途中收集更多實體戳印、車票票根。
- **手札風旅遊雜記頁 (Ruled Travel Notes)**：
  - **上半部相片黏貼處**：提供無框相片預留區，可點擊叫出「資產圖庫選擇器」無損加載高解析照片。
  - **下半部橫欄筆記區**：提供模擬日系筆記本橫線背景（Ruled Lines）的互動文字輸入框，打字時自動將文字即時同步存入 LocalStorage。
- **加頁管理與列印自適應**：
  - 網頁端提供「🗑️ 刪除此頁」互動按鈕，供用戶隨時移除加頁並自動跳轉（列印時自動隱藏）。
  - 優化列印樣式，自訂加頁會自動以 A5 二摺對稱分頁排版，偶數頁自動強制斷頁 (`page-break-after: always`)，完美相容 landscape A4 列印規格。

### 4.7 - 雙頁版列印與預覽左右分頁修復與購物備忘擴增 (Print Split-Page Layout Fix & Spending Table Extension V4.7)
*發布日期：2026-06-14*
- **修復列印時「刷卡攻略」與「行李清單」未切分左右頁 Bug**：修復在 `template-detailed` 模式下列印時，由於列印媒體 `@media print` 缺少 `grid-template-columns` 等布局參數，導致頁面內容由並排雙 A5 面板退化為單欄垂直堆疊（如圖一）的排版 Bug。
- **補全列印媒體網格布局樣式**：在 `@media print` 的 `body.template-detailed #page-cards` 與 `#page-packing` 樣式中完整補齊 `grid-template-columns: 1.15fr 1fr !important` 與各子元素位置屬性（如圖二），使列印輸出與網頁預覽完全一致。
- **極大化實際消費記錄表格 (22 行)**：將靜態消費與購物備忘清單表格由 14 行增加至 22 行（如圖二），充分利用 A5 (210mm) 的垂直列印高度，在維持「絕不跨頁超限」的安全前提下，提供極大化的手寫記帳空間。

### 4.6 - 高解析圖庫檔案選擇器與自訂路徑套用 (High-Res Gallery Asset Selector V4.6)
*發布日期：2026-06-14*
- **無損畫質解決方案**：移除了原本會降低清晰度的 Canvas 壓縮與 Base64 二進位上傳機制。改為讓使用者直接從本地 `assets/` 資料夾中讀取無損的高解析度原始影像檔案，完全解決列印時相片模糊的問題。
- **資產圖庫彈出式選擇器**：點擊封面或每日日程相片時，會彈出精心設計的「資產圖庫彈出視窗」。內含 `assets` 資料夾中所有圖片的縮圖清單（如 `bird_day6.png` 等），點選即可瞬間無痛套用。
- **彈性的自訂路徑欄位**：如果在彈出式圖庫中找不到剛放進 `assets` 的檔案，可於視窗下方的「自訂相片路徑」輸入框中直接鍵入檔案名稱（例如：`assets/my_new_photo.png`），點擊「套用路徑」即可立即載入。

### 4.5 - 自訂照片上傳、自動畫布縮放與 LocalStorage 壓縮儲存 (Custom Image Upload & Compression V4.5)
*發布日期：2026-06-14*
- **自訂照片上傳機制**：封面照片與各日程頁面照片全面升級為「可點擊互動」。用戶點擊任何照片即可彈出本機檔案選擇器，自由選擇圖庫內的照片。
- **自動畫布縮放與品質壓縮 (Canvas 800px & JPEG 0.7)**：為防止大尺寸相片超出瀏覽器 LocalStorage 5MB 的儲存上限，上傳相片時會自動在背景透過 HTML5 Canvas 將解析度縮小至最大邊長 `800px`（維持原比例），並進行品質設為 `0.7` 的 JPEG 壓縮。壓縮後的圖片極小（約 20KB~50KB），能夠安全儲存於 LocalStorage，即使有十天行程與多張相片也不會超限。
- **滑鼠懸停更換提示與動態反饋**：封面照片與日程照片容器在滑鼠懸停時會顯示「📷 點擊更換照片」浮動提示，並加入精美的微縮放與明暗對比過渡效果，提升使用者引導和互動體驗。

### 4.4 - 東京車站第六日精華行程與自訂插圖 (Tokyo Station Day 6 Itinerary & Illustration V4.4)
*發布日期：2026-06-14*
- **新增第六天東京車站精華日程**：調整原先 5 日行程，將旅行日期延伸為 6 日（與封面日期 `2026.06.15 一 — 06.20 六` 完美貼合）。調整第五天（Day 5）結尾至飯店休息，將原先的回程機場安排與免稅伴手禮採購獨立成第六天（Day 6）完整的一日行程。
- **全新東京車站小胖主角插圖**：導入全新手繪風格 cockatiel Mascot (小胖) 於東京車站前敬禮的日系水彩插圖 (`assets/bird_day6.png`)。當選擇「小胖」主角時，Day 6 將會載入該專屬主題相片。
- **動態 Mascot 載入與 fallback 機制**：透過 JS 主角載入與 `onerror` 機制，其餘主角（如狗、貓、蛇、熊貓）在載入不存在的第 6 天相片時，將自動優雅降級 fallback 至 Day 1 相片，不影響整體閱讀與發生破圖，保持系統高度相容性。
- **修復雙頁版「刷卡攻略」與「行李清單」全時顯示 Bug**：修復在 `template-detailed` 模式下，因 CSS 優先級強套 `display: grid !important`，導致這兩個頁面在非啟動狀態下仍強行顯示在其他日期下方的 Bug。修改為僅在含有 `.active` class 時以 grid 顯示，並於列印媒體 `@media print` 內保留列印版面樣式。

### 4.3 - 旅行相片自適應相框寬度包裹與無損顯示優化 (Dynamic Photo Polaroid Frame Auto-Wrap V4.3)
*發布日期：2026-06-14*
- **拍立得相框自適應寬度包裹 (fit-content)**：將日常拍立得相框 `.day-hero-image` 寬度調整為 `fit-content`。當相片寬度較小時（例如直向/正方形照片），相框會自動縮窄以貼合包裹相片，不再強制拉寬到 100% 寬度而在左右兩側產生大塊多餘白邊，極致優化視覺質感。
- **橫向與直向照片雙重完美佈局**：
  - 對於橫向照片，圖片會拉寬並填滿整個相框寬度，不留左右白邊；
  - 對於直立式照片，其高度會縮放至 `250px`，相框隨之收窄包裹，並在頁面中保持居中。兩種比例照片皆能百分之百完整無損展示，不產生任何裁剪。
- **交通說明卡片無感下移**：交通與說明卡片會隨照片高度自然下移，在 A5 頁面內合理分佈空間，保證所有行程資訊在一頁內完整收納，不發生跨頁或重疊。

### 4.2 - 封面照片放大、旅行照片自適應無損顯示與小夥伴命名修正 (Cover Photo Enlarge & Daily Photo Contain Fit V4.2)
*發布日期：2026-06-14*
- **雙頁版封面大照大器放大**：將雙頁詳細版下的封面照片大小由 `180px x 180px` 大幅放大至 `360px x 360px`，解決封面圖偏小、視覺不平衡的問題，使其在列印時展現更具視覺衝擊力的封面。
- **每日行程照片自適應無損顯示 (object-fit: contain)**：調整 Page A 的拍立得日常照佈局，為圖片與相框設定 `height: auto` 與 `object-fit: contain !important;` 屬性。使得任何比例（尤其是橫向極寬的 panoramic banner 圖片）能百分之百完整在 A5 拍立得相框中顯示，不再被左右裁剪或卡掉。
- **旅行小夥伴小名修正**：修改 `index.html` 的 Mascot 選項與 `app.js` 中的 `MASCOT_CONFIG`，將 `玄鳳小胖` 修正為 `小胖`，使封面夥伴關係標籤（如：小胖 & 家祥 🐦）更為親切簡潔。

### 4.1 - 雙頁詳細版重構為 A4 橫向二折頁版型 (Detailed Template Landscape A4 2-Up Refactoring V4.1)
*發布日期：2026-06-14*
- **雙頁詳細版橫向二摺化**：將原本雙頁詳細版（直式 A4，每天佔 2 頁 A4，造成列印成本高昂）重構為 A4 橫向列印 (`A4 landscape; margin: 0;`)，每個 Sheet 左右對稱擺放 2 個 A5 面板（寬 `148.5mm`，高 `210mm`），印出對摺即可裝訂成精巧手冊。
- **刷卡攻略與消費備忘左右切分**：重組 Page 2 刷卡攻略頁面的 CSS Grid。左半頁（A5）擺放中信 Uniopen & 玉山熊本熊卡雙卡說明與疊加技提示，右半頁（A5）完整展示 14 行的「實際消費記錄與備忘」手記記帳表格。
- **行李打包與航班住宿左右切分**：重組 Page 3 行李打包頁面的 CSS Grid。左半頁（A5）包含重要證件/3C/穿搭/盥洗分類清單與添加自訂物品欄，右半頁（A5）垂直排版「去回程航班資訊」與「住宿飯店資訊」，高度利用橫向空間。
- **每日行程 A5 垂直重構**：Page A 在 A5 窄版頁面下，自動將拍立得日常照與交通說明/小貼士卡片由左右佈局調整為「上方大照、下方交通」垂直流佈局，照片與字體按比例微調收攏；Page B 放置滿版 timeline 景點簡介。
- **封面與戳章頁 A5 化**：封面頁與紀念戳章收集頁縮小為 148.5mm A5 面板。在列印時，封面位於 Sheet 1 左半，Stamps 位於 Sheet 9 左半，保證摺疊拼合後的頁面結構一致。
- **手冊預覽 (Booklet Preview) 虛擬折線與拼頁**：在預覽模式下為 A5 左右對頁中間加上 dashed 折疊線，並在 Day B 結束處自動加入 `margin-right: 40px` 視覺空白代表 Sheet Break，提供極度擬真的紙張拼頁體驗。

### 4.0 - 四大版型重磅登場與 A4 橫向四折風琴口袋手冊 (Four Layout Templates & A4 4-Fold Accordion Booklet V4.0)
*發布日期：2026-06-14*
- **新增四大排版版型**：
  - **📐 單頁經典版 (compact)**：1日1頁經典圖文。左上相框，右上交通，中下滿寬行程。
  - **📖 雙頁詳細版 (detailed)**：1日2頁，將每日行程重構為 Page A（標題/大圖/交通）與 Page B（大寬版景點 Timeline 簡介），提供超大文字書寫空間，不虞擁擠。
  - **📝 單頁強效純文字版 (text-heavy)**：1日1頁無圖，隱藏拍立得大照片，將版面全數釋放給交通與景點文字。
  - **🗂️ 四折頁口袋版 (fourfold)**：將 A4 橫向紙張等分為 4 個直欄（寬 `74.25mm`，高 `210mm`），以極致輕量化折疊長條口袋書規格輸出。
- **口袋手冊折紙輔助線與自動換頁**：
  - 每欄之間繪製一條精緻的**灰色虛線 (border-right: 1px dashed)** 作為實體折紙摺疊輔助線。
  - 每 4 個面板（剛好印滿一面 A4 橫向）自動插入 page-break (`page-break-after: always`)，第 4 欄自動隱藏折線，確保列印 PDF 不會產生錯位與多餘空白頁。
- **口袋版元素全面縮小與窄版自適應**：
  - 當切換至口袋版時，所有橫向 Grid（Cover Meta、雙卡攻略、行李清單、航班住宿及每日行程佈局）在 print/preview 下自動重構為單欄垂直流。
  - 主角封面圖等比縮小至 `45mm` 高度，每日拍立得相框限高 `120px`，字體與內距全面等比微調縮小，防止窄欄寬度破版或文字溢出。
- **動態印表機 @page 配置 (Dynamic Printer Orientation & Margin CSS)**：
  - 在前端 JS 中動態維護 `<style id="print-page-style">` 標籤。當切換為四折口袋版時，自動向瀏覽器發送 `@page { size: A4 landscape !important; margin: 0 !important; }`，切回其他版型時則自動還原為 `@page { size: A4 portrait; margin: 12mm 15mm; }`，實現全自動、無感式的跨平台列印方向調整。
- **頁面跳轉導航優化**：
  - 將每日行程錨點 ID 與 nav 統一後綴為 `-a`，在 `app.js` 的 `switchPage` 邏輯中，當點擊詳細版 Day 頁時，自動一併將 Day A 與 Day B 設為 active，使用戶在互動網頁模式中能以直覺的垂直捲動方式流暢閱讀兩頁內容。

### 3.19 - 優化直立式照片高度與封面圖大器放大 (Vertical Photo Height Support & Enlarge Cover Photo V3.19)
*發布日期：2026-06-14*
- **修復直立式/正方形日常照片被截斷**：針對 Day 1 和 Day 2 的正方形或直立式小夥伴插圖，由於原本 V3.18 設置 of `max-height: 220px` 限制太低，導致圖片底部被截斷、無法印出完整拍立得白色外框。我們將網頁與列印端的相框與相片最大高度上限大幅放寬至 `380px`（相片 `360px`），確保直立式與正方形照片等比例無損完整顯示。同時中下的行程簡介欄位（寬版）依然能有充足空間容納多達 4 站以上的行程，不會造成版面壓縮。
- **封面主角照片顯著放大**：依據第三張對照圖，將列印時封面圖片容器 `.cover-image-container` 的尺寸從 `240px x 240px` 顯著放大至 `360px x 360px`，並微調圓角為 `16px`。這能將封面主角（玄鳳小胖等）在列印 PDF 中以大氣、吸睛的視覺比例呈現，使手冊第一頁更具視覺衝擊力。

### 3.18 - 修復相片列印被截斷與比例自適應優化 (Auto-Height Polaroid & Print Overflows Fix V3.18)
*發布日期：2026-06-14*
- **修復橫向相片列印被截斷問題**：在列印樣式中，將原本在 V3.17 中硬編碼為固定 `175px` 的拍立得相框 `.day-hero-image` 高度以及設定為 `100%` 的 `.day-art` 圖片高度全部改為自適應的 `height: auto !important;`。這能使任何比例（如 16:9 或 3:2）的橫向照片按自身比例在列印時被放大至對應高度展示，絕不在圖片底部或四周產生白色截斷，完整印出拍立得外框。
- **引入相片安全高度上限限制（防止擠壓交通與行程）**：為了確保照片等比放大後不會過度膨脹進而壓縮或擠退「交通說明欄」與「中下行程欄」，在網頁端與列印端對拍立得大相框與內部圖片設定了合理的 `max-height` 安全上限（列印端最大高限制在 `220px`/`200px`，網頁端限制在 `250px`/`230px`）。對於橫向照片，能完全等比例無損顯示；對於正方形照片，在超出此高度上限時則由 `object-fit: cover` 優雅微裁剪，極致保障手冊版面不被向下推移。

### 3.17 - 每日行程排版大改版：左上圖片、右上交通、中下寬版景點簡介 (Day Page Layout Overhaul: Top-Left Photo, Top-Right Transport, Full-Width Bottom Timeline V3.17)
*發布日期：2026-06-14*
- **每日行程全新格局調整**：為了解決景點簡介文字較多時、在窄欄中容易縱向溢出的問題，並消除圖片左右兩側的無用留白，將每日行程頁（Day 1 - 5）改為兩列式上下雙層結構：
  - **上層（左上+右上）**：採用網格排版，左上擺放拍立得大相片 `.day-hero-image`（拉大尺寸最大寬度改為滿寬），右上擺放今日交通與說明及小貼士卡片 `.day-sidebar`。如此圖片可最大化放大展現（像原先一樣大），又不會在兩側留下大塊空白。
  - **下層（中下滿寬）**：將大篇幅的景點簡介/行程規劃 timeline `.day-bottom-timeline` 移至下半部並橫向拉寬至 100% 寬度。這為較長的旅遊資訊文字提供了更寬廣的水平排版空間，文字能更流暢地向左右流動，縱向高度顯著縮小，極致降低了列印時溢出斷頁的風險。
- **時空連線對齊最佳化**：在滿寬的景點簡介中，`.no-time` 模式下的時間連線仍保持完美左對齊，使景點與景點之間的閱讀邏輯依然連貫且極具裝飾感。

### 3.16 - 解決 Booklet-Preview 列印斷頁與頁數加倍 bug (Preview Print Overflows & Page-Doubling Fix V3.16)
*發布日期：2026-06-14*
- **修復列印斷頁與 A4 頁數溢出 bug (關鍵修復)**：先前用戶在瀏覽器中開啟「手冊預覽模式」（`body` 被加上 `.booklet-preview` 類別）時直接列印，該模式下的 screen 樣式（例如 `.book-page` 的 `min-height: 297mm;` 與 `padding: 18mm 15mm !important;`）因為 CSS 權重較高，會直接覆蓋 `@media print` 內的列印樣式，導致在列印時每個頁面被強行加上 A4 滿高及大量內邊距。與瀏覽器內建的列印邊界（如 12mm/15mm）疊加後，總高度大於實體 A4 紙張，迫使瀏覽器將每個頁面腰斬分割，甚至連同 `PAGE BREAK (A4)` 水印也被印在紙上，頁數暴增至 17 頁。
- **限制預覽樣式僅在 screen 生效**：將所有 `body.booklet-preview` 相關的排版規則全部以 `@media screen` 媒體查詢包裹。這樣做可確保預覽模式只在螢幕上呈現，當觸發真實列印時，瀏覽器會完全忽略螢幕預覽樣式，回歸使用最純淨且已嚴密限高微調的 `@media print` 列印樣式（`.book-page` 高度 `238mm`），完美將 5 天手冊控制在精確的 **9 頁**，無任何多餘斷頁。
- **封面高度自適應調整**：在列印樣式中，將 `.cover-layout` 的高度從原本的固定 `228mm` 改為自適應 parent 容器的 `100% !important;`，配合 `justify-content: space-between` 能更柔和且有彈性地在實體 A4 的範圍內分配垂直空間，杜絕因版面元素高度微差被擠壓到下一頁的風險。

### 3.15 - 每日行程欄位對調、無時間景點簡介與航班住宿卡片整合 (Layout Swap & Flight Lodging Integration V3.15)
*發布日期：2026-06-14*
- **每日行程欄位對調**：依據實體手冊排版截圖，將「景點簡介（原本的行程 Timeline）」與「今日交通與貼士（原本的左側欄）」順序對調。大螢幕下左側為 1fr 寬的景點簡介，右側為 280px 的交通與貼士；列印時右側對調為 200px 的交通說明。
- **無時間景點簡介與左對齊**：為配合「行程照舊不用排時間，改成每個點的簡介」需求，Timeline 渲染時不再輸出時間區段。同時新增 `.no-time` 樣式，將連線從 `left: 80px` 移至安全的 `left: 4px` 完美靠左對齊，把省下來的寬度全數讓給景點簡介，大幅增加印刷的可讀空間。
- **航班與住宿資訊整合至 Page 3**：在 Page 3 行李清單頁最下方新增航班資訊與住宿飯店展示卡片，不佔用封面的主視覺空間，且在 A4 列印時能完美填滿下方留白，手冊總頁數依然維持精確的 **9 頁**。
- **基本設定表單欄位升級**：升級編輯器的基本設定分頁，加入去回程航班（時間/機場/航空公司/航班號）與兩家住宿飯店的獨立輸入框，免除手動改 JSON 的繁瑣。
- **LocalStorage 資料轉移支援**：在網頁加載時自動對舊有 LocalStorage 檔案進行欄位遷移，主動補齊 `flights` 與 `hotels` 預設欄位，防止用戶既有數據失效或崩潰。
- **列印斷頁優化與圖片高度限制 (9頁預算防溢出)**：為了解決高解析度或直式封面與每日行程插圖在列印時因超出高度而被鋸斷、並在下方產生空白頁的問題，在列印樣式中為 `.cover-image-container` 指定固定尺寸（`240px x 240px` 完美正方形），並將每日行程頁的拍立得卡片 `.day-hero-image` 的列印高度嚴格限制在 `150px`（搭配 `object-fit: cover` 裁剪與 `max-width: 250px`）。同時微調戳章收集頁的高度為 `178mm`，確保 5 天的旅行手冊在任何列印設定下皆能穩妥、精確地維持在 **9 頁** A4 紙張內，徹底杜絕分頁倍增！

### 3.7 - 支援自訂旅行小夥伴名字與基本設定欄位擴充 (Custom Traveler Name & Editor Extension V3.7)
*發布日期：2026-06-14*
- **自訂小夥伴姓名**：在編輯手冊的「基本設定」中，新增「您的名字」輸入欄位。用戶可將預設的「你」更改為自己的真實姓名（例如：小明）。
- **封面小夥伴動態渲染**：修改手冊封面中「旅行小夥伴」的更新邏輯。現在系統會結合當前所選的主角與您設定的名字進行動態渲染（例如：`玄鳳小波 & 小明 🐦` 或是 `馬爾濟斯 & 小明 🐶`），讓您的實體手冊更具專屬的個人紀念價值。

### 3.6 - 新增玄鳳小波主角選項與消費備忘表格極致擴增 (Cockatiel Mascot Xiao Bo & 14-Row Table V3.6)
*發布日期：2026-06-14*
- **新增主角「玄鳳小波」 (🐦)**：在主角選擇器中新增第五位旅行小夥伴 —— Q版玄鳳鸚鵡「小波」（Cockatiel）。系統完美融入「小波」相關主題與專屬封面標誌，並在前端程式碼中配置專屬圖片路徑與防崩潰載入邊界。在圖像生成 quota 重設後將自動載入完整的 Q 版特色插圖（黃身體、橘色腮紅、滿滿的頭頂羽冠、不禿頭）。
- **封面防失真載入邊界**：封面圖片同樣增加 `onerror` 容錯，當小波封面插圖加載中時，將自動優雅地降級載入馬爾濟斯封面，不出現任何破圖圖標，確保使用者體驗不受影響。
- **消費記錄表大幅拉長至 14 行**：將雙卡攻略下方的「實際消費記錄與備忘」表格行數由 8 行極致擴大至 **14 行**，完全填滿整個 A4 紙張的下半部（給下方留空），徹底拉滿手寫便利度與美觀度。

### 3.5 - 解決 A4 雙頁列印 Bug、封面大圖、非裁剪拍立得與消費表記錄大幅擴增 (A4 Page Break Fix & Clean Polaroid V3.5)
*發布日期：2026-06-14*
- **消除雙頁列印 (重大修復)**：移除 `.book-page` 容器在 print 下的 `page-break-after: always !important`。此前同時保留 `page-break-before` 與 `page-break-after` 導致瀏覽器在相鄰分頁標記之間強行插入空白頁（造成頁數翻倍）。現在僅保留 `page-break-before: always`，徹底解決 blank page 溢出！
- **自然頂部對齊 (flex-start 佈局)**：
  - 將列印時 `.book-page` 容器的對齊方式從 `space-between` 改為 `flex-start`。這使得卡片攻略、打包清單等元素自然靠上對齊，多餘空間將優雅地留在底端（給下方留空），不再產生奇怪的上下拉扯大空白。
  - 行李清單與卡片攻略在列印時，依然強制保持精緻的雙欄並列版面，完美利用橫向面積。
- **大幅增加消費記錄欄位**：將「實際消費記錄與備忘」手寫記帳表格從原先的 3 行大幅擴增至 **8 行**。同時將上方的信用卡卡片與神技提示框在縱向上往上提拉，徹底填補原有的中間留白區域，將空間最大化利用。
- **拍立得相框加大且絕不裁剪 (object-fit: contain)**：
  - **相片原汁原味呈現**：將網頁與列印的日常照片 `.day-art` 設為 `aspect-ratio: 1/1 !important` 且以 `object-fit: contain` 展示，配合 Polaroid 的 padding 外框，使可愛的水彩插畫（1:1 比例）百分之百完整呈現，絕不砍掉任何插圖邊緣，也不留下左右的白色留白條。
  - **列印圖片尺寸調整**：應配置比例將列印時拍立得 `.day-hero-image` 寬度加大至 `280px` (與網頁一致)，保證 PDF 中的插畫顯得大氣且比例協調。
  - **行程與 Timeline 提拉**：配合 `.day-layout` 頂部 flex-start 對齊與細緻的 Timeline 行高字體微調，大相片與詳細行程均能穩妥容納在單一 A4 內。
- **封面圖片比例加大**：將封面圖片 container 寬度調大至 `360px`（網頁）/ `360px`（列印），使其填補多餘空白，讓第一頁主視覺非常震撼且比例均衡。
- **純白無框戳章蓋章專區**：最後一頁的 `.blank-stamp-area` 蓋章區在列印時強制啟用 `background-color: #FFFFFF !important`，保留頂部標題的和紙底色，而下方的收集部分維持乾淨無雜質的實體純白，便於實體戳章的收集與色彩還原。

### 3.4 - 日系和紙漸層背景、封面佈局精簡與緊湊列印優化 (Washi Gradient & Compact Print V3.4)
*發布日期：2026-06-14*
- **質感日系漸層背景 (不單調的白色)**：為所有手帳頁面（`.book-page`，包括實體戳章頁）引進精心調配的暖色系日系和紙漸層背景 `linear-gradient(135deg, #FAF6F0 0%, #EFE7DC 100%)` 並與 `radial-gradient` 網點疊加。列印時若啟用「列印背景圖形」，實體手冊將呈現細緻、溫潤的淡雅和紙色澤，不再是單調無趣的死白。
- **封面佈局大精簡與拉高**：
  - 移除封面的副標題（「日本旅遊雙卡刷卡攻略&五日精華行程」）及「✈️ 去程航班」項目。這能大幅釋放封面縱向空間，將旅行日期與小夥伴卡片顯著往上提拉，但仍體面地保持在下方，並讓可愛的封面主角插畫完整且大器地展示。
  - 將日期與夥伴資訊改為精美的雙欄並列版面，背景更換為溫暖的陶橘淡色卡片樣式（`var(--primary-light)`），精緻度倍增。
- **拍立得日常相框比例與 timeline 提拉**：
  - **解決圖片切邊/留白條**：在網頁端將拍立得相框（`.day-hero-image`）最大寬度從 `480px` 限制在緊湊的 `280px`，並指定圖片以 `220px` 比例進行正方形裁剪，完美展現 `1:1` 的主角插畫，且左右不再殘留奇怪的白色空邊。
  - **行程大幅上提**：將日常拍立得插圖的上下 margin 縮小，釋放的縱向高度自動將下方的「今日交通與說明」與「行程規劃 timeline」往上推，避免重要行程因排版擠壓被切除。
- **安全列印高度 buffer (徹底消滅 18 頁)**：
  - 將列印時 `.book-page` 容器的高度從 `260mm` 進一步下調至最安全的 `245mm`。這為 Safari / Chrome 默認勾選的「列印標頭與註腳」預留了充足的邊界緩衝，配合精簡的封面與緊湊的 timeline 字型（`9.5pt`），在任何列印設定下都保證 **動態天數手冊剛好 4 + N 頁** (5天行程精確列印為 9 張紙)，絕對不溢出任何空白頁。

### 3.3 - 徹底解決列印翻倍溢出與純白戳章收集頁 (No Page-Doubling & Blank Stamps Page)
*發布日期：2026-06-14*
- **消除列印翻倍，固定 A4 列印高度**：移除列印時 `.book-page` 容器的底邊距（`margin-bottom: 0 !important`），並微調列印頁面高度至安全的 `260mm`。這徹底解決了瀏覽器在每一頁之後強行產生空白頁、導致列印頁數翻倍（從 9 頁變成 18 頁）的問題。
- **微調插畫列印尺寸防止超頁**：將封面主角插畫的列印最大寬度調降為 `300px`（並將其 margin 減至 `20px`），並將每日行程頁的 Hero Image 容器與水彩插圖的列印最大高度分別限縮在 `140px` 與 `130px`。這保證了 4 + N 頁手冊在列印時，每一頁皆完美容納在單一 A4 紙張內，絕不因高度溢出而擠壓產生額外頁面。
- **行李清單自由刪除與改名**：正式支援行李物品的線上自主編修。現在用戶可以雙擊物品文字來修改名稱，並點選右側的紅色 **`×`** 按鈕將其刪除（該刪除按鈕具備 `.no-print` 類別，在列印 PDF 時會自動隱藏，維持實體手冊的精緻與簡潔）。
- **純白無框戳章收集頁**：將最後一頁的紀念戳章收集區改為完全無邊框、無格線、無圓圈的純白大畫布（`.blank-stamp-area`），僅保留最上方標題，供旅客在線下自由收集不同尺寸、異形設計的精美景點/車站紀念章。

### 3.1 - 排版校正與主角動態插圖同步 (Layout Correction & Mascot Sync)
*發布日期：2026-06-14*
- **移除每日手札，確保精確 9 頁**：徹底移除 Day 1 - 5 底部的手寫日記區與小型蓋章框，排除高度溢出問題，使紙張列印在 PDF 中精準符合 **9 頁** 預算。
- **拍立得圖片完整呈現**：修改圖片 CSS 容器為 `object-fit: contain` 並拉高 max-height，使每日行程的 banner 水彩插圖完整呈現、不再攔腰切斷。
- **加大封面主角插圖**：封面圖最大寬度擴增至 `340px`，解決第一頁大面積空白與主視覺偏小的空泛感。
- **主角更換全頁插圖連動**：在封面更換主角（🐶馬爾濟斯 / 🐱貓貓 / 🐍綠蛇 / 🐼熊貓）時，Day 1 - 5 頁面的行程插圖會同步動態載入對應主角的系列插圖 (`assets/${mascot}_day[1-5].png`)。
- **純淨大紀念章收集頁**：第 9 頁重新設計為**純淨整頁蓋章專區**，移除記事行與網格線背景，改為 4 個帶有復古花紋與戳章標示的大圓/長方形收集框。

### 2.1 - 手帳排版美化與空間利用優化 (Polaroid & Washi Tape & Stamp Boxes)
*發布日期：2026-06-13*
- **手帳網格背景 (Hobonichi Grid)**：為每一頁加入溫馨的點陣網格線，營造真實實體筆記本 (手帳) 的精緻質感。
- **拍立得相框與紙膠帶裝飾 (Polaroid & Washi Tape)**：將每日日程的Banner重塑為**拍立得照片效果**（白色邊框、相紙陰影、有機的微小偏角傾斜），並在頂部黏貼半透明的**紙膠帶 (washi tape)**，增添精美手作剪貼感。
- **解決版面留白空間 (Fill Underutilized Page Space)**：
  - **行李清單頁**：在底部新增「🛒 伴手禮與藥妝待買清單 (Shopping Wishlist)」實體格。
  - **刷卡攻略頁**：將「實際消費記錄」手寫表格擴增為 6 行。
  - **每日行程頁 (Day 1 - 5)**：在每頁底部追加「✍️ 每日旅遊手帳日記與蓋章框」，包含實體手寫線與紀念戳章框，將列印的空白版面轉換為極具紀念價值的親手書寫手寫區！
- **列印對稱拉伸**：調整 A4 預覽及列印尺寸比例，使內容自動上下拉伸，消除大面積空底，完美利用 A4 紙張空間。

### 2.0 (重大更新) - 多國手冊管理器 & 標楷體列印優化
*發布日期：2026-06-13*
- **多手冊管理器**：實作手冊切換選單、新增手冊及刪除手冊功能。資料均存在 LocalStorage。
- **雙模式編輯器**：支援基本表單修改，以及進階 JSON 導入/導出。
- **標楷體字型**：CSS 導入 Mac `BiauKai` 與 Windows `DFKai-SB (標楷體)`，使實體印出的繁體中文筆畫更加清晰。
- **嚴格 9 頁 A4 列印預算**：調整列印媒體查詢，限縮圖片高度（`140px`）與邊距，確保 9 大核心頁面在列印時各自剛好佔用 1 頁 A4 紙，印出總數恰好為 9 頁。

### 1.1 - 主角主題切換與插圖生成
*發布日期：2026-06-13*
- **多主角支援**：新增 🐶 馬爾濟斯、🐱 溫馨貓貓、🐍 萌萌綠蛇、🐼 圓滾熊貓 四種主角選擇。
- **插圖更換邏輯**：為新增的主角各生成專屬的櫻花鐵塔封面圖，切換時即時變更封面、標題與 Emoji。

### 1.0 - 靜態東京 2026 手冊基底
*發布日期：2026-06-13*
- **頁面結構**：包含封面頁、雙卡攻略、行李清單、Day 1-5 行程規劃、記事蓋章頁。
- **水彩插圖**：生成 6 張馬爾濟斯🐶系列水彩插圖。
- **互動工具**：實作倒數計時、刷卡推薦計算器、行李打包進度條。
